task printMap {
    def map = ['width': 1024, 'height': 720]
    map.getClass().name

    println map['width']
    println map.height

    map.each {
        println "Key:$it.key,Value:$it.value"
    }
}